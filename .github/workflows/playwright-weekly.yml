name: Weekly Playwright E2E

on:
  schedule:
    - cron: '0 3 * * 0' # At 03:00 UTC every Sunday
  workflow_dispatch: {}

jobs:
  weekly-e2e:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '18'

      - name: Install dependencies
        run: npm ci

      - name: Install Playwright browsers
        run: npx playwright install --with-deps

      - name: Start backend and static server (background)
        run: |
          node ./server/index.js &
          echo "backend_pid=$!" > /tmp/backend.pid
          python3 -m http.server 8000 &
          echo "static_pid=$!" > /tmp/static.pid
          for i in {1..30}; do
            if curl -sSf http://localhost:4000/api/leaderboard >/dev/null 2>&1 && curl -sSf http://localhost:8000/standalone.html >/dev/null 2>&1; then
              echo "services are up"; break
            fi
            sleep 1
          done

      - name: Run Playwright via runner script
        run: node ./scripts/run-playwright-with-backend.js

      - name: Upload Playwright artifacts (on failure)
        if: failure()
        uses: actions/upload-artifact@v4
        with:
          name: playwright-weekly-artifacts
          path: |
            test-results/**
            playwright-report/**
            trace.zip

      - name: Cleanup
        if: always()
        run: |
          if [ -f /tmp/backend.pid ]; then kill "$(cat /tmp/backend.pid)" || true; fi
          if [ -f /tmp/static.pid ]; then kill "$(cat /tmp/static.pid)" || true; fi
